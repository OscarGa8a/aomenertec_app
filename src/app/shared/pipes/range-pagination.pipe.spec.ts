import { RangePaginationPipe } from './range-pagination.pipe';

describe('RangePaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new RangePaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
