import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aoymenertec.app',
  appName: 'aoym-enertec',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
